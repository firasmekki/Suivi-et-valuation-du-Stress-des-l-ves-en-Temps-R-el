// ** Default export for handling menu group visibility
export const canViewMenuGroup = item => {
  // Par défaut, on autorise tous les groupes de menu
  // Vous pouvez ajouter ici votre logique de vérification des permissions
  return true
}

// ** Check if user can view menu item
export const canViewMenuItem = item => {
  // Par défaut, on autorise tous les éléments de menu
  // Vous pouvez ajouter ici votre logique de vérification des permissions
  return true
} 